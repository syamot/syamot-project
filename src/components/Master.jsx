import React, { useState, useEffect, useContext } from "react";
// const URL =
//    process.env.NODE_ENV === "production"
//      ? `/stones/${playID}`
//      : `http://localhost:8000/stones/${playID}`;

const Master = () => {
  return <h1>masterConection</h1>;
};

export default Master;
